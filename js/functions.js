//CRIANDO TABELAS APOS CLICAR ADD
var createData = {

  //Inserindo Linhas
  insertLine: function(){
    var table = document.getElementById("collection").innerHTML

    //Inserindo Linhas - onde irá ficar os dados dos PRODUTOS
    var row = collection.insertRow(numberRows);
  }

var data = document.getElementById('collection');

var row = collection.insertRow(1);
row.innerHTML = ""+name+" "+price+";
};
